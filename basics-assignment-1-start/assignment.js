const userInput = 2; // recuerda que si un contenedor de datos no va a cambiar, es CONSTANTE, no variable
let result;

result = userInput + 28;
result -= 6;
result *= 2;
result /= 4;

alert(userInput);
alert(result);